# NodeJS y MongoDB con Docker

Utilizando contenedores de Docker para ejecutar Node y Mongo para una API de prueba.

`docker-compose build`: para construir la app

`docker-compose up`: para ejecutarla
